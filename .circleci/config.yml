version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      JVM_OPTS: -Xmx3200m
      TERM: dumb
    steps:
      - checkout
      - run: echo "hello world"
  
  push-image:
    machine: true
    ######################
    #docker:
    #  - image: circleci/node:4.8.2
   # working_directory: ~/repo
    #environment:
      # Customize the JVM maximum heap limit
     # JVM_OPTS: -Xmx3200m
     # TERM: dumb
    ######################
    steps:
      - checkout
      #- run: echo "hello world" #remove this later
      - run: pwd
      - run: ls -latr
      - run: echo ${CISCO_AUTH} > ${HOME}/gcp-key.json
      - run: docker build --rm=false -f ./dir/Dockerfile -t containers.cisco.com/sunrajam/test/${IMAGE_NAME}:$CIRCLE_SHA1 ./dir
    #  - run: docker build --rm=false -t containers.cisco.com/sunrajam/${CNAAP_PROJECT}/${IMAGE_NAME}:$CIRCLE_SHA1 .
    # - run: gcloud auth activate-service-account --key-file ${HOME}/gcp-key.json
    # - run: gcloud --quiet config set project ${GCP_PROJECT}
    # - run: gcloud docker -- push eu.gcr.io/${GCP_PROJECT}/${IMAGE_NAME}:$CIRCLE_SHA1
      - run: docker push containers.cisco.com/sunrajam/test/${IMAGE_NAME}:$CIRCLE_SHA1

workflows:
  version: 2
  build_and_push:
    jobs:
      - build
      - push-image:
          requires:
            - build
